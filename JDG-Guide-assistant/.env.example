POSTGRES_DB=jdg_assistant
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_HOST=postgres
OPENAI_API_KEY=your-key-here

# Source repo for the guide content that gets ingested (owner/name + branch).
JDG_REPO=sobolevbel/jdg
JDG_BRANCH=master

# Grafana admin login (dashboards themselves are viewable without logging in).
GF_ADMIN_PASSWORD=admin
